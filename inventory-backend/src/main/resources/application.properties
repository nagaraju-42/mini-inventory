# =============================================
# DATABASE CONFIG (Internal for Render Deployment)
# =============================================

# We use the internal hostname here because both the App and DB are on Render.
# Internal connections on Render do NOT require sslmode=require.
spring.datasource.url=jdbc:postgresql://dpg-d75rjhjuibrs73btd1bg-a:5432/inventory_db_udqt

spring.datasource.username=inventory_db_udqt_user
spring.datasource.password=Mk8fiGR4vYpjcvOK1sXrQvlxb5N9ACqI
spring.datasource.driver-class-name=org.postgresql.Driver

# =============================================
# JPA / HIBERNATE CONFIG
# =============================================
# 'update' will create tables automatically if they don't exist
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# =============================================
# SERVER PORT
# Render sets the PORT environment variable automatically, 
# but we'll default to 8080.
# =============================================
server.port=${PORT:8080}